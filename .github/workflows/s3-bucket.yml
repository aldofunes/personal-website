name: Production Build

on:
  pull_request:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: NPM Install
        run: |
          npm ci
      - name: Production Build
        run: |
          npm run build

      - name: S3 Sync
        uses: ItsKarma/aws-cli@v1.70.0
        with:
          args: aws s3 help
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
      # - name: S3 Sync
      #   uses: ItsKarma/aws-cli@v1.70.0
      #   with:
      #     args: |
      #       aws s3 sync public s3://{{ secrets.AWS_S3_BUCKET }} --cache-control max-age=31536000,public --exclude index.html;
      #       aws s3 cp public/index.html s3://{{ secrets.AWS_S3_BUCKET }}/index.html --metadata-directive REPLACE --cache-control max-age=0,no-cache,no-store,must-revalidate --content-type text/html
      #   env:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}